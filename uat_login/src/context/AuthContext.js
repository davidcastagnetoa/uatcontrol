// Import the React library
import React from "react";

// Create an AuthContext using React.createContext() and initialize it with a null value
const AuthContext = React.createContext(null);

// Export the AuthContext so it can be used in other components
export default AuthContext;
